# Sun-ice — HANDOFF (поточний стан, "на чому зупинились")

> Статика (стек, архітектура, конвенції, ролі, технічні пастки) — у `CLAUDE.md` в
> корені репозиторію, Claude Code читає його автоматично на старті сесії. Тут —
> тільки те, що змінюється: що вже готово, що в роботі, відомі відкриті питання.
> Детальна хронологія кожної зміни — `claude/CHANGELOG.md`.
>
> Цей файл МІГ відстати від реального коду, якщо користувач правив щось без участі
> Claude. Перед тим як спиратись на конкретне твердження — звір grep'ом по
> `index.html`/`sw.js`, а не вір документації наосліп.

## Останнє оновлення

2026-09-10 — CRМонтаж, третя правка того ж дня: блок "Додати постачальника" виділено
кольоровим боксом (губився під списком постачальників), і обидва поля боргу (клієнта в
монтажі, постачальника в розділі "Постачальники") стали вільним текстом (`type="text"`,
колонки в БД `numeric`→`text`) замість лише числа — можна писати "1500 + доставка" тощо.
**Чекає заливання**: `index.html` на GitHub + ДВІ SQL-міграції по черзі в Supabase SQL
Editor — спершу `claude/sql/2026-09-10-installer-tasks-client-debt-and-suppliers.sql`
(якщо ще не виконана), потім `claude/sql/2026-09-10-installer-debt-fields-to-text.sql`.
Найранішній `2026-09-10-installer-tasks-debt-deposit.sql` — НЕ виконувати, він
скасований. Деталі — CHANGELOG 2026-09-10 (три записи поспіль).

2026-09-10 — CRМонтаж, друга правка того ж дня: "борг" розділено на два окремі поняття.
Борг **клієнта** — лишився в самій формі монтажу як одне просте поле "Борг клієнта"
(колонка `client_debt_amount` в `installer_tasks`, старі `debt_amount`/`debt_note`/
`deposit_amount`/`deposit_note` прибрані як помилковий перший варіант). Борг
**постачальникам** (монтажник сам винен, постачальників може бути кілька) — винесено в
новий окремий розділ "🏭 Постачальники" (кнопка в тулбарі CRМонтаж), нова таблиця
`installer_suppliers`, проста поточна сума на постачальника без журналу операцій.
Форма "Новий монтаж" також отримала поле "Вид робіт" (Монтаж/Демонтаж/Закладка/Навіска/
Чистка/Сервіс замість категорій обладнання), галочку "Виконано" поруч зі статусом у
картці, і компактнішу верстку (була розтягнута через `white-space: pre-wrap`).

2026-09-10 — Плитки каталогу: експеримент із сірою студійною підложкою на всю плитку
(CSS-градієнт + mix-blend-mode, за замовчуванням для всіх фото-плиток) ПОВНІСТЮ
відкочений на прохання користувача — не сподобалась лінія-шов між відтінками. Дизайн
плиток знову такий, як був до цього експерименту (фото в білому боксі з рамкою).
Єдине, що лишилось із того експерименту — плитка "Спліт-системи" тепер показує
реальне фото MHI SRK25ZS-WT (замість старого стокового фото), без жодної підложки чи
композиції з другим брендом. Деталі й повна історія експерименту (навіщо починали, що
саме не спрацювало) — CHANGELOG 2026-09-10 (три записи поспіль).

2026-09-10 — CRМонтаж спрощено: прибрано роль `installer` і фічу "напарник" (спільні
монтажі між двома інсталяторами). Розділ тепер стаціонарно доступний будь-якому
залогіненому користувачу, незалежно від ролі, як особистий список монтажів. Деталі —
CHANGELOG 2026-09-10.

## Статус модулів

- **Каталог, Акції, Кабінет, Контакти, Інфо** — стабільні, без відомих відкритих
  задач станом на цю дату.
- **CRМонтаж** — стаціонарний розділ для будь-якого залогіненого користувача: список
  монтажів (CRUD), поле "Вид робіт" (Монтаж/Демонтаж/Закладка/Навіска/Чистка/Сервіс),
  просте поле "Борг клієнта" в самому монтажі, окремий розділ "🏭 Постачальники" (список
  постачальників з поточною сумою боргу монтажника кожному з них — не прив'язано до
  конкретних монтажів), .ics-нагадування, друк/Excel-експорт, відправка в месенджери,
  галочка "Виконано" поруч зі статусом у картці. Роль `installer` і фіча "напарник"
  прибрані як зайве ускладнення. Не реалізовано: push-нагадування для
  Android/десктопу (опційна частина початкового ТЗ) — наступний крок лише якщо
  користувач попросить окремо.

## Статус плиток каталогу (10 штук)

**Наповнені даними і робочою логікою (5 з 10):**
1. Спліт-системи — mhi+gal (аркуші ПОБУТОВІ / GALACTIC)
2. Мульти спліт-системи — mhi+gal (МУЛЬТИСИСТЕМИ / GALACTIC)
3. Напівпромислові спліт-системи — mhi+gal (НАПІВПРОМ / Galactic LCAC)
4. VRF (Мультизональні) — `custom: 'vrf'`, власний парсинг
5. Теплові насоси — `custom: 'heatpumps'`, власний парсинг

**Порожні заглушки "Скоро" (5 з 10):** ККБ (компресорно-конденсаторні блоки), Чиллери,
Вентиляційне обладнання, Повітряні завіси, Конвектори та обігрівачі. Дані для них ще не
узгоджені (незрозуміло, які аркуші прайсу їм відповідають) — потрібне уточнення від
користувача, коли/якщо ці плитки стануть пріоритетом.

## Відкриті питання / TODO

- **Перший реальний super_admin (Артем)** — за старими нотатками ще НЕ був
  зареєстрований і не підвищений вручну через SQL. **Перевірити цей факт заново** на
  старті будь-якої нової сесії, яка торкається ролей/адмінки — стан міг змінитись без
  оновлення цього файлу.
- **Google Play Protect блокує встановлення на Samsung** ("Небезпечний додаток
  заблоковано"). Найімовірніше — задокументований баг у мінтері WebAPK браузера
  Samsung Internet (проставляє застарілу службову версію Android). Рекомендація:
  встановлювати через Google Chrome, а не Samsung Internet. Це НЕ проблема коду
  застосунку, окремого рішення не потрібно.

## Що робити на старті нової сесії

1. `CLAUDE.md` (корінь репо) читається автоматично — там стек, конвенції, ролі,
   технічні пастки.
2. Прочитати цей файл (поточний статус) і за потреби `claude/CHANGELOG.md` (деталі
   конкретних змін, дати).
3. Не покладатись сліпо на документацію для точних деталей коду — звірити ключові
   твердження grep'ом по `index.html`/`sw.js`.
4. Стан SQL-схеми, RLS-політик і Edge Functions у Supabase з цього репо НЕВІДОМИЙ —
   уточнити в користувача, що саме вже виконано в Supabase Dashboard, якщо це
   важливо для задачі.
