# Sun-ice — HANDOFF (поточний стан, "на чому зупинились")

> Статика (стек, архітектура, конвенції, ролі, технічні пастки) — у `CLAUDE.md` в
> корені репозиторію, Claude Code читає його автоматично на старті сесії. Тут —
> тільки те, що змінюється: що вже готово, що в роботі, відомі відкриті питання.
> Детальна хронологія кожної зміни — `claude/CHANGELOG.md`.
>
> Цей файл МІГ відстати від реального коду, якщо користувач правив щось без участі
> Claude. Перед тим як спиратись на конкретне твердження — звір grep'ом по
> `index.html`/`sw.js`, а не вір документації наосліп.

## Останнє оновлення

2026-09-15 — Усі 8 наповнених плиток каталогу (не тільки Спліт-системи) тепер самі
перегортаються — `flipImg` додано до кожного запису `CATALOG_TILES`. Механізм
узагальнено на будь-яку кількість плиток одночасно: `startFlip()`/`stopAllFlips()`
(було `startSplitFlip`/`stopSplitFlip` — лишились тільки в CHANGELOG як історія).
7 нових фото — від користувача (`D:\Клод\Зображення на плитки\`, кожне назване як
плитка на екрані), приведені до 1200×900 на білому. Відповідність фото → плитка і
повний список нових файлів — CHANGELOG 2026-09-15. **Чекає заливання**: `index.html`
і 7 нових файлів у `tile-images/`.

2026-09-14 — Плитка "Спліт-системи" тепер сама перегортається (flip) раз на 5-7 сек між
фото MHI SRK25ZS-WT і Galactic GKZ09M-W (нове фото, знайдене на офіційному galactic.ua),
як жива плитка Windows — `CATALOG_TILES['split'].flipImg` + `startSplitFlip()`/
`stopSplitFlip()` в `index.html`. Зупиняється по кліку на будь-яку плитку каталогу.
Легкий механізм (один `setTimeout`, GPU-анімація), навантаження не додає. Деталі,
зокрема нюанс зі скриншот-інструментом Browser-пейна (не сам застосунок) — CHANGELOG
2026-09-14. **Чекає заливання**: `index.html` і нове фото
`tile-images/tile-split-galactic.jpg`.

2026-09-14 — Живий огляд застосунку користувачем (GitHub Pages) → 2 виправлення в
`index.html`: (1) перенос артикулу FRICO/REMAK посеред слова — фактична причина була
не лише в CSS `.row-name`, а в тому, що `.row-price` з приміткою "· роздрібна" з'їдав
занадто багато ширини рядка (`flex-shrink: 0` в `.row-right`) — примітку винесено в
окремий рядок (`row-price-note`); (2) пошук у Systemair/Повітряні завіси тепер працює
з російською/українською розкладкою клавіатури не лише для однозначних літер, а й для
неоднозначних (в/н/р/с/у — де вигляд і звучання розходяться) через regex-клас з обома
варіантами. Деталі й що саме тестовано — CHANGELOG 2026-09-14. Свідомо НЕ займали
(за проханням користувача): старі 4 фото-плитки на GitHub, `tile-catalogs.jpg`,
JOKE_MODE. **Чекає заливання**: тільки `index.html`.

2026-09-14 — Користувач підтвердив живцем після заливання `index.html`: CRМонтаж і
"Сумісність блоків MHI" (Інфо) працюють як задумано, зауважень немає. SQL-міграції
(`2026-09-10-installer-tasks-client-debt-and-suppliers.sql`,
`2026-09-10-installer-debt-fields-to-text.sql`) перевірені й виконані в Supabase —
питання закрито, `client_debt_amount` тепер `text`, таблиця `installer_suppliers`
існує.

⚠️ **Не забути залити на GitHub** (готові локально, у гілці попереду origin, але ще не
на GitHub — звірено `git diff HEAD origin/main` 2026-09-14): 4 фото-плитки
(`tilevrf.jpg`, `tile-multisplit.jpg`, `tileheatpumps.jpg`, `tile-split.jpg` —
без них там і далі старі версії зі шво́м-градієнтом/чужим фото Galactic), плюс
`claude/CHANGELOG.md`, `claude/HANDOFF.md` і 3 SQL-файли (вже виконані в Supabase,
заливаються на GitHub лише для довідки/історії, не впливають на роботу застосунку).

2026-09-14 — Синхронізація git: злито локальні напрацювання (CRМонтаж повністю, сумісність
блоків MHI, чисті фото-плитки) з роботою іншого сеансу на іншому комп'ютері (нові плитки
каталогу — ККБ, Вентиляційне обладнання/Systemair, Повітряні завіси/FRICO-REMAK, підбренд
теплових насосів MHI EZY/Q-ton, фото серій із вбудованих в Excel картинок). **Важливо**:
той інший сеанс залив на GitHub `index.html` зі старої локальної копії, яка ще НЕ мала
CRМонтажу взагалі (жодного сліду форми монтажу/постачальників) — тобто напряму на GitHub
(до цього мерджу) CRМонтаж був відсутній. Мердж це виправив: обидві функціональності тепер
в одному файлі. Реальний код-конфлікт був лише у функції `openSeriesInfo()` — обидві сторони
розширили її третій аргумент по-різному (фото серії vs кнопка-дія для таблиці сумісності);
об'єднано в одну функцію, що приймає або масив фото, або opts-об'єкт. Деталі — CHANGELOG
2026-09-14. **Чекає заливання**: `index.html` (єдиний файл з реальними код-змінами
мерджу — решта конфліктів були тільки в документації, не в коді).

2026-09-11 — CRМонтаж, форма "Новий монтаж": телефон клієнта тепер тільки цифрами,
дата/час розбито на окремі поля (дата — нативний календар, час — ручний ввід цифрами
з автопідстановкою ":"), у полі "Борг клієнта" з'явилась кнопка "OK" для візуального
підтвердження введеного. Деталі — CHANGELOG 2026-09-11. **Чекає заливання**: тільки
`index.html`.

2026-09-11 — Плитки каталогу: фон 6 фото-плиток (`tilevrf.jpg`, `tile-multisplit.jpg`,
`tileheatpumps.jpg`, `tilecompat.jpg`, `tilecompathousehold.jpg`, `tilecompatsemi.jpg`)
очищено від власного сірого/блакитного градієнта до чистого білого — щоб на плитці не
було видно нічого, крім контуру, фото продукту й назви. `heatpumpsmhi.jpg`
(лайфстайл-фото зі стіною/підлогою) очистити автоматично НЕ вдалось — лишили як є,
потрібне інше фото або ручна обробка, якщо це стане пріоритетом. Деталі, метод і що
саме заливати — CHANGELOG 2026-09-11. **Чекає заливання**: 6 файлів у `tile-images/`
(перелік вище), `index.html` не змінювався.

2026-09-10 — CRМонтаж, третя правка того ж дня: блок "Додати постачальника" виділено
кольоровим боксом (губився під списком постачальників), і обидва поля боргу (клієнта в
монтажі, постачальника в розділі "Постачальники") стали вільним текстом (`type="text"`,
колонки в БД `numeric`→`text`) замість лише числа — можна писати "1500 + доставка" тощо.
**Чекає заливання**: `index.html` на GitHub + ДВІ SQL-міграції по черзі в Supabase SQL
Editor — спершу `claude/sql/2026-09-10-installer-tasks-client-debt-and-suppliers.sql`
(якщо ще не виконана), потім `claude/sql/2026-09-10-installer-debt-fields-to-text.sql`.
Найранішній `2026-09-10-installer-tasks-debt-deposit.sql` — НЕ виконувати, він
скасований. Деталі — CHANGELOG 2026-09-10 (три записи поспіль).

2026-09-10 — CRМонтаж, друга правка того ж дня: "борг" розділено на два окремі поняття.
Борг **клієнта** — лишився в самій формі монтажу як одне просте поле "Борг клієнта"
(колонка `client_debt_amount` в `installer_tasks`, старі `debt_amount`/`debt_note`/
`deposit_amount`/`deposit_note` прибрані як помилковий перший варіант). Борг
**постачальникам** (монтажник сам винен, постачальників може бути кілька) — винесено в
новий окремий розділ "🏭 Постачальники" (кнопка в тулбарі CRМонтаж), нова таблиця
`installer_suppliers`, проста поточна сума на постачальника без журналу операцій.
Форма "Новий монтаж" також отримала поле "Вид робіт" (Монтаж/Демонтаж/Закладка/Навіска/
Чистка/Сервіс замість категорій обладнання), галочку "Виконано" поруч зі статусом у
картці, і компактнішу верстку (була розтягнута через `white-space: pre-wrap`).

2026-09-10 — Плитки каталогу: експеримент із сірою студійною підложкою на всю плитку
(CSS-градієнт + mix-blend-mode, за замовчуванням для всіх фото-плиток) ПОВНІСТЮ
відкочений на прохання користувача — не сподобалась лінія-шов між відтінками. Дизайн
плиток знову такий, як був до цього експерименту (фото в білому боксі з рамкою).
Єдине, що лишилось із того експерименту — плитка "Спліт-системи" тепер показує
реальне фото MHI SRK25ZS-WT (замість старого стокового фото), без жодної підложки чи
композиції з другим брендом. Деталі й повна історія експерименту (навіщо починали, що
саме не спрацювало) — CHANGELOG 2026-09-10 (три записи поспіль).

2026-09-10 — CRМонтаж спрощено: прибрано роль `installer` і фічу "напарник" (спільні
монтажі між двома інсталяторами). Розділ тепер стаціонарно доступний будь-якому
залогіненому користувачу, незалежно від ролі, як особистий список монтажів. Деталі —
CHANGELOG 2026-09-10.

## Статус модулів

- **Каталог, Акції, Кабінет, Контакти, Інфо** — стабільні, без відомих відкритих
  задач станом на цю дату.
- **CRМонтаж** — стаціонарний розділ для будь-якого залогіненого користувача: список
  монтажів (CRUD), поле "Вид робіт" (Монтаж/Демонтаж/Закладка/Навіска/Чистка/Сервіс),
  просте поле "Борг клієнта" в самому монтажі, окремий розділ "🏭 Постачальники" (список
  постачальників з поточною сумою боргу монтажника кожному з них — не прив'язано до
  конкретних монтажів), .ics-нагадування, друк/Excel-експорт, відправка в месенджери,
  галочка "Виконано" поруч зі статусом у картці. Роль `installer` і фіча "напарник"
  прибрані як зайве ускладнення. Не реалізовано: push-нагадування для
  Android/десктопу (опційна частина початкового ТЗ) — наступний крок лише якщо
  користувач попросить окремо.

## Статус плиток каталогу (10 штук)

**Наповнені даними і робочою логікою (8 з 10)** — три нові додані в сесії на іншому
комп'ютері 2026-09-14, злито в CHANGELOG 2026-09-14:
1. Спліт-системи — mhi+gal (аркуші ПОБУТОВІ / GALACTIC)
2. Мульти спліт-системи — mhi+gal (МУЛЬТИСИСТЕМИ / GALACTIC)
3. Напівпромислові спліт-системи — mhi+gal (НАПІВПРОМ / Galactic LCAC)
4. VRF (Мультизональні) — `custom: 'vrf'`, власний парсинг
5. Теплові насоси — `custom: 'heatpumps'`, власний парсинг (+ підбренд MHI EZY/Q-ton)
6. ККБ (компресорно-конденсаторні блоки) — `custom: 'kkb'`
7. Вентиляційне обладнання — `custom: 'ventilation'`, бренд Systemair (аркуш SYSTEMAIR,
   пошук товару на офіційному сайті за SKU — `openSystemairSkuPage()`)
8. Повітряні завіси — `custom: 'aircurtains'`, перемикач брендів FRICO/REMAK (аркуші
   FRICO / REMAK ЗАВІСИ)

**Порожні заглушки "Скоро" (2 з 10):** Чиллери, Конвектори та обігрівачі. Дані для них ще
не узгоджені — потрібне уточнення від користувача, коли/якщо ці плитки стануть
пріоритетом.

## Відкриті питання / TODO

- **Перший реальний super_admin (Артем)** — за старими нотатками ще НЕ був
  зареєстрований і не підвищений вручну через SQL. **Перевірити цей факт заново** на
  старті будь-якої нової сесії, яка торкається ролей/адмінки — стан міг змінитись без
  оновлення цього файлу.
- **Google Play Protect блокує встановлення на Samsung** ("Небезпечний додаток
  заблоковано"). Найімовірніше — задокументований баг у мінтері WebAPK браузера
  Samsung Internet (проставляє застарілу службову версію Android). Рекомендація:
  встановлювати через Google Chrome, а не Samsung Internet. Це НЕ проблема коду
  застосунку, окремого рішення не потрібно.

## Що робити на старті нової сесії

1. `CLAUDE.md` (корінь репо) читається автоматично — там стек, конвенції, ролі,
   технічні пастки.
2. Прочитати цей файл (поточний статус) і за потреби `claude/CHANGELOG.md` (деталі
   конкретних змін, дати).
3. Не покладатись сліпо на документацію для точних деталей коду — звірити ключові
   твердження grep'ом по `index.html`/`sw.js`.
4. Стан SQL-схеми, RLS-політик і Edge Functions у Supabase з цього репо НЕВІДОМИЙ —
   уточнити в користувача, що саме вже виконано в Supabase Dashboard, якщо це
   важливо для задачі.
